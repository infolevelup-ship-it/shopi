# Supabase — Project Settings → API en supabase.com
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Nunca exponer al navegador. Solo usado server-side (src/lib/supabase/service-role.ts).
SUPABASE_SERVICE_ROLE_KEY=

# Siigo — usados solo desde el backend (doc 06). Nunca en el navegador.
SIIGO_USERNAME=
SIIGO_ACCESS_KEY=
SIIGO_PARTNER_ID=

# GoHighLevel — usados solo desde el backend (doc 07). Nunca en el navegador.
GHL_PRIVATE_TOKEN=
GHL_LOCATION_ID=
GHL_WEBHOOK_SECRET=
